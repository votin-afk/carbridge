# CarBridge - Платформа для импорта авто из Китая

**Последнее обновление:** 11.03.2026

## Оригинальное техзадание
Комплексная платформа для импорта автомобилей из Китая в Беларусь. Включает:
- Главная страница с каталогом, калькулятором, AI-ассистентом
- Личный кабинет пользователя: гараж, заявки, тендеры, сделки, верификация
- Панель модератора: управление пользователями, документами, сделками
- Система подрядчиков: регистрация, дашборд
- **Интеграция с Bitrix24** для автоматизации CRM

## Роли пользователей
- **user**: Обычный пользователь (test@test.com / test)
- **moderator**: Модератор для проверки документов и сделок
- **admin**: Полный доступ (votin@tut.by / test)
- **contractor**: Подрядчик (test.contractor@test.com / test123)

## Этапы сделки
1. Лизинг (опционально)
2. Инспекция авто
3. Выкуп и экспорт
4. Доставка до порта (Китай)
5. Страхование авто
6. Доставка в Беларусь
7. Таможенное оформление
8. Завершение сделки

---

## ✅ Реализовано по ТЗ (11.03.2026)

### ✅ КЛИЕНТ (Пункты 1-8)

**Пункт 1 - Регистрация клиента**
- Форма: Имя*, Фамилия*, Телефон*, Город, Email, Пароль, Реферальный код
- Контакт автоматически создаётся в Bitrix24 с полными данными

**Пункт 2 - Гараж с кнопками действий**
- 4 кнопки: "Добавить в сделку", "Запустить тендер", "Помощь менеджера", "Выбрать подрядчика"
- Визуальные статусы: Верификация, Предоплата $500, Договор
- Кнопки disabled до верификации + предоплаты

**Пункт 3 - Верификация клиента с предоплатой**
- 5 шагов: Данные → Документы → Договор → Предоплата $500 → Подтверждение
- Вкладка с реквизитами для оплаты
- Договор с чекбоксом "Я согласен"

**Пункт 4 - Заявки с обязательной ссылкой**
- Обязательное поле "Ссылка на понравившееся авто" (выделено голубым)
- Валидация при отправке

**Пункт 5 - Помощь менеджера**
- Раздел Документы с 3 табами: Сделки, Помощь менеджера, Юридическая помощь
- Чат с менеджером
- Кнопка "Запросить звонок"

**Пункт 6 - Назначение подрядчиков на этапы**
- Выбор подрядчика для каждого этапа сделки
- Уведомление подрядчику при назначении
- Создание сделки в Bitrix24

**Пункт 7 - Раздел Документы со структурой сделки**
- Чат по сделке с подрядчиком
- Обмен файлами по этапам
- Завершение этапа и изменение стоимости

**Пункт 8 - Тендеры**
- Запуск тендера из заявки
- Предложения подрядчиков с фото/видео
- Выбор предложения и создание сделки

### ✅ ПОДРЯДЧИК

- Регистрация с указанием услуг
- Личный кабинет с табами: Обзор, Тендеры, Заявки, Сделки, Предложения
- Форма предложения по тендеру:
  - Ссылка на автомобиль
  - Детали автомобиля
  - Фото и видео (ссылки)
  - Этапы сделки в предложении с ценами
  - Срок действия предложения

### ✅ МОДЕРАТОР

- Управление пользователями с колонками:
  - Пользователь (имя, фамилия, телефон)
  - Email, Роль, Баланс
  - **Предоплата** (кнопка "Подтвердить")
  - **Верификация**
  - Действия (Профиль, Роль, Удалить)
- Одобрение подрядчиков
- Модерация этапов сделок
- Управление документами

### ✅ BITRIX24 ИНТЕГРАЦИЯ

- Создание контактов при регистрации
- Создание лидов при запросах помощи
- Создание сделок при назначении подрядчиков
- Webhook: https://b24-m0jhiy.bitrix24.by/

---
- Фильтрация по этапам

### ✅ Зеркальный раздел в кабинете подрядчика - РЕАЛИЗОВАНО
- Вкладка "Мои сделки" в ContractorDashboard
- Чат с клиентом
- Просмотр и загрузка файлов

---

## Рефакторинг server.py (08.12.2025 - 09.03.2026)

### ✅ Создана модульная архитектура backend

**Задача**: Разбить монолитный server.py (8043 строки) на модули.

**Выполнено (09.03.2026)**:
1. Исправлена ошибка bcrypt 3.2.0 -> 4.1.3 (устранена `AttributeError: module 'bcrypt' has no attribute '__about__'`)
2. **Создан роутер каталога** - `routes/catalog.py` (полностью рабочий):
   - Эндпоинты: /brands, /models/{brand}, /search, /{car_id}, /{car_id}/add-to-garage
   - Интеграция с Che168 API
   - Fallback на статические данные
3. **Создан сервис Che168** - `services/che168.py`:
   - Класс Che168API для работы с API каталога
   - Кэширование запросов
   - Парсинг и нормализация данных
4. **Удалён дублирующийся код из server.py**:
   - Удалены эндпоинты каталога (~550 строк)
   - Удалён статический каталог CHINESE_CAR_CATALOG (~270 строк)
   - Удалён класс Che168API (~340 строк)

**Результат**: server.py сокращён с 8043 до 6882 строк (~14% уменьшение)

**Статус**: IN PROGRESS

**Следующие шаги**:
1. Вынести роутеры: deals, contractors, moderator, tenders, garage
2. Вынести ProAuctionsParser в отдельный сервис
3. Постепенно подключать модули и удалять дублирующийся код

---

## Исправленные баги (08.12.2025)

### ✅ Баг: Предложения подрядчиков не отображались покупателю
**Проблема**: Когда подрядчик отправлял предложение на тендер через `/api/contractor-offers`, оно сохранялось в коллекцию `tender_offers`, но покупатель видел только mock-предложения из документа тендера.

**Причина**: Эндпоинты `GET /api/tenders` и `GET /api/tenders/{tender_id}` возвращали только `offers` из документа тендера, не подтягивая реальные предложения из коллекции `tender_offers`.

**Решение**:
1. Обновлён `GET /api/tenders` — теперь для каждого тендера загружаются предложения из `tender_offers` и объединяются с mock offers
2. Обновлён `GET /api/tenders/{tender_id}` — аналогично
3. Обновлён `POST /api/tenders/{tender_id}/select/{offer_id}` — теперь проверяет и mock offers, и реальные предложения
4. Обновлены эндпоинты заявок `GET /api/applications/my` и `GET /api/applications/{id}` — добавлена загрузка предложений

**Файлы изменены**: `/app/backend/server.py`

---

## Исправленные баги (08.03.2026)

### ✅ Баг: Заявки отображались как "Любая марка" в тендерах
**Проблема**: После создания заявки с указанием марки/модели, в разделе тендеров отображалось "Любая марка" вместо реальных данных.

**Причина**: 
1. Модель `TenderResponse` не включала поля `car_request` и `application_id`
2. Фронтенд (Tenders.js, ContractorDashboard.js) искал данные только в `car_info`, игнорируя `car_request`

**Решение**:
1. Добавлены поля `car_request` и `application_id` в `TenderResponse`
2. Фронтенд теперь проверяет оба источника: `tender.car_info?.brand || tender.car_request?.brand`

---

## Backend API (сообщения и файлы)

### Для клиента:
- `GET /api/deals/{deal_id}/messages` - получить сообщения
- `POST /api/deals/{deal_id}/messages` - отправить сообщение
- `GET /api/deals/{deal_id}/files` - получить файлы
- `POST /api/deals/{deal_id}/files` - загрузить файл
- `GET /api/deals/{deal_id}/files/{file_id}/download` - скачать файл
- `DELETE /api/deals/{deal_id}/files/{file_id}` - удалить файл

### Для подрядчика:
- `GET /api/contractor/deals` - получить мои сделки
- `GET /api/contractor/deals/{deal_id}/messages` - получить сообщения
- `POST /api/contractor/deals/{deal_id}/messages` - отправить сообщение
- `GET /api/contractor/deals/{deal_id}/files` - получить файлы
- `POST /api/contractor/deals/{deal_id}/files` - загрузить файл
- `GET /api/contractor/deals/{deal_id}/files/{file_id}/download` - скачать файл

---

## Текущая архитектура

### Backend
- **Framework**: FastAPI
- **Database**: MongoDB
- **Auth**: JWT токены (отдельные для пользователей и подрядчиков)
- **File**: `/app/backend/server.py` (~7700+ строк) - **РЕФАКТОРИНГ В ПРОЦЕССЕ**

### Frontend
- **Framework**: React
- **Styling**: Tailwind CSS + Shadcn UI
- **State**: React Context (AuthContext)

### Структура проекта (После рефакторинга 08.12.2025)
```
/app
├── backend/
│   ├── config.py              # ✅ НОВЫЙ: Конфигурация приложения
│   ├── database.py            # ✅ НОВЫЙ: MongoDB подключение
│   ├── server.py              # Основной API (7700+ строк) - рефакторинг в процессе
│   ├── requirements.txt
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py         # ✅ НОВЫЙ: Pydantic модели
│   ├── routes/
│   │   ├── __init__.py
│   │   ├── auth.py            # ✅ НОВЫЙ: Роуты аутентификации
│   │   ├── affiliate.py       # ✅ НОВЫЙ: Роуты партнёрской программы
│   │   ├── user.py            # ✅ НОВЫЙ: Роуты пользователя
│   │   └── leasing.py         # ✅ НОВЫЙ: Роуты лизинга
│   ├── services/
│   │   ├── __init__.py
│   │   └── calculator.py      # ✅ НОВЫЙ: Сервис калькулятора
│   ├── utils/
│   │   ├── __init__.py
│   │   ├── auth.py            # ✅ НОВЫЙ: Утилиты аутентификации
│   │   └── cache.py           # ✅ НОВЫЙ: Утилиты кэширования
│   └── tests/
│       └── test_refactoring_validation.py  # ✅ НОВЫЙ: Тесты рефакторинга
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── dashboard/
│   │   │   │   ├── DashboardOverview.js
│   │   │   │   ├── MyGarage.js
│   │   │   │   ├── DealCars.js
│   │   │   │   ├── Applications.js      ✅ Полностью переработано
│   │   │   │   ├── Tenders.js
│   │   │   │   ├── Verification.js
│   │   │   │   ├── Documents.js
│   │   │   │   └── Tracking.js
│   │   │   ├── ContractorDashboard.js
│   │   │   ├── ModeratorPage.js
│   │   │   └── ...
│   │   ├── layouts/
│   │   │   └── DashboardLayout.js
│   │   └── contexts/
│   │       └── AuthContext.js
│   └── package.json
└── memory/
    └── PRD.md
```

---

## Выполненные задачи (Декабрь 2025 - Март 2026)

### ✅ Интеграция каталога с Che168 API - ЗАВЕРШЕНО
**Дата**: 06.03.2026

Интегрирован реальный каталог автомобилей с крупнейшей китайской площадки б/у авто che168.com через API auto-api.com.

**API конфигурация:**
- Base URL: `https://api1.auto-api.com/api/v2/che168`
- API Key: `DQugK90Bo5ci1ZeDP6Wr`

**Реализованные эндпоинты:**
- `GET /api/catalog/search` - Поиск авто с фильтрами (марка, модель, год, цена, тип двигателя, кузов)
- `GET /api/catalog/brands` - Список всех марок (100+ брендов)
- `GET /api/catalog/models/{brand}` - Модели для конкретной марки

**Данные с Che168:**
- Реальные цены в юанях (CNY)
- Пробег в км
- Тип двигателя (ДВС/Гибрид/Электро)
- Тип кузова (седан, кроссовер, хэтчбек и др.)
- Цвет автомобиля
- Локация продавца (город в Китае)
- VIN-код, мощность, КПП
- Ссылка на оригинальное объявление

**Frontend обновления:**
- Главная страница: секция "Авто из Китая" с бейджем CHE168
- Страница каталога: полный поиск с фильтрами
- Карточки авто: цена, пробег, год, цвет, локация, кнопка "В гараж"

**Тестирование:** ✅ 100% (20/20 backend тестов, все frontend элементы работают)

---

### ✅ Форма заявки на подбор автомобиля - ПОЛНОСТЬЮ ПЕРЕРАБОТАНА
**Дата**: 06.03.2026

Форма заявки на подбор автомобиля (`/dashboard/applications`) полностью переработана согласно PDF-шаблону пользователя.

**Новая структура формы (6 разделов):**

**Раздел 1: Данные клиента**
- Тип клиента (физ/юр лицо)
- ФИО / Наименование компании
- Город доставки

**Раздел 2: Параметры автомобиля**
- 2.1 Основные характеристики: марка, модель, год (от/до), тип кузова
- 2.2 Двигатель и трансмиссия: тип двигателя, объём, мощность (от/до), тип КПП, привод
- 2.3 Внешний вид: цвет кузова, точный цвет, важность цвета, цвет салона, материал салона

**Раздел 3: Пробег и состояние**
- 3.1 Пробег: максимальный пробег, состояние авто
- 3.2 Допустимые повреждения: чекбокс, уровни повреждений (1-4), комментарий

**Раздел 4: Дополнительные опции**
- Электронные системы: Система 360°, ACC, LKA, автопарковка, датчики, камера, HUD, CarPlay
- Комфорт: панорама, подогрев/вентиляция/массаж сидений, климат-контроль, электробагажник
- Внешний вид: спортпакет, диски R18+, LED/Matrix фары, тонировка
- Прочее: фаркоп, запаска, третий ряд, premium аудио

**Раздел 5: Бюджет и условия**
- Бюджет в Китае (от/до USD)
- Общий бюджет до (с доставкой и таможней)
- Срок покупки: срочно, 1 месяц, 2-3 месяца, не спешу
- Способ оплаты: 100% предоплата, рассрочка, кредит/лизинг
- Назначение: личное, бизнес, такси, перепродажа
- Таможенная очистка: через CarBridge, самостоятельно, не знаю

**Раздел 6: Приоритеты и комментарии**
- Ранжирование 1-5: цена, надёжность, технологии, престиж, экономия топлива
- Дополнительные пожелания
- Сводка заявки

**Действия после создания заявки:**
- Запросить помощь менеджера в подборе ($200)
- Запустить тендер
- Отменить заявку

**Тестирование:** ✅ Полностью протестировано (16/16 backend тестов, все 6 шагов frontend)

---

### ✅ Предыдущие выполненные задачи

- Калькулятор таможенных платежей с Указом №140
- 8-этапный процесс сделки (лизинг, проверка, экспорт, логистика, страхование, доставка, таможня, завершение)
- GPS-отслеживание авто (mock)
- Публичная страница подрядчиков с ценами услуг
- Дашборд подрядчика

---

## Бэклог задач

### P1 - Высокий приоритет

1. **Интеграция Stripe для пополнения баланса**
   - Подключить Stripe API
   - Создать эндпоинт для обработки платежей
   - Обновить UI кнопки "Пополнить"

2. **AI чат на главной странице - надёжность**
   - Исследовать причины нестабильности
   - Улучшить error handling
   - Добавить retry логику

3. **Доработка раздела "Тендеры"**
   - UI для сравнения и принятия предложений от подрядчиков

### P2 - Средний приоритет

1. **URL парсер для гаража**
   - Заменить httpx на более надёжное решение
   - Рассмотреть scraping API или Playwright

2. **Рефакторинг кода** ✅ ПЕРВЫЙ ЭТАП ЗАВЕРШЁН
   - ✅ Создана модульная структура (routes, models, services, utils)
   - ✅ Созданы роутеры: auth.py, affiliate.py, user.py, leasing.py
   - ✅ Создан сервис калькулятора
   - 🔄 В процессе: создание остальных роутеров (deals, contractors, catalog, moderator)
   - Текущий размер server.py: ~7700 строк

### P3 - Низкий приоритет / Backlog

1. GPS трекинг доставки авто (реальные данные)
2. Уведомления Telegram/Email
3. Система эскроу платежей
4. Расширенная аналитика для модераторов

---

## Ключевые константы

```python
DEAL_ADD_FEE = 300      # $300 за добавление авто в сделку (бесплатно из тендера)
CONSULTANT_FEE = 200    # $200 за помощь консультанта/менеджера
```

---

## Исправленные баги

### ✅ Баг: Подрядчик не мог отправить предложение на тендер - ИСПРАВЛЕНО
**Дата**: 07.03.2026

**Проблема**: При попытке отправить предложение на тендер появлялась ошибка "Неверный токен" (invalid token), несмотря на успешный вход в систему.

**Корневая причина**: В `ContractorDashboard.js` переменная `token` инициализировалась один раз при загрузке компонента через `localStorage.getItem('contractor_token')`. После входа в систему токен сохранялся в localStorage, но React-компонент продолжал использовать старое значение (NULL), так как это была обычная переменная, а не state.

**Решение**:
1. Преобразовали `token` из обычной переменной в React state: `const [token, setToken] = useState(() => localStorage.getItem('contractor_token'))`
2. Обновили `ContractorLogin` чтобы передавать новый токен в `onSuccess(newToken)`
3. Обновили `fetchDashboard(currentToken)` чтобы принимать токен как параметр
4. Добавили обновление `setToken(null)` при logout и ошибке 401

**Файлы изменены**: `/app/frontend/src/pages/ContractorDashboard.js`

---

### ✅ Полная детализация заявок для подрядчиков - РЕАЛИЗОВАНО
**Дата**: 07.03.2026

**Задача**: Подрядчик должен видеть полную информацию о заявке клиента со всеми деталями.

**Решение**:
1. Расширен список заявок с краткой информацией (бюджет Китай/общий, год, двигатель, кузов, привод, пробег, цвет, сроки, желаемые опции)
2. Добавлена кнопка "Все детали" для открытия диалога с полной информацией
3. Диалог детализации включает:
   - Информация о клиенте (имя, тип, телефон, email, город доставки)
   - Требования к автомобилю (марка, модель, год, кузов, двигатель, мощность, КПП, привод, пробег, состояние)
   - Цветовые предпочтения (кузов, салон, важность цвета)
   - Бюджет и оплата (бюджет в Китае, общий, способ оплаты, сроки, растаможка)
   - Приоритеты клиента (цена, надёжность, технологии, престиж, экономия топлива)
   - Желаемые опции (комфорт, электроника, экстерьер, прочее)
   - Дополнительные требования

**Файлы изменены**: `/app/frontend/src/pages/ContractorDashboard.js`

---

## Тестовые данные

- **Admin User**: test@test.com / test
- **Contractor**: Все подрядчики имеют пароль `contractor123`
- **API URL**: https://vehicle-tender-hub.preview.emergentagent.com/api

---

## Ключевые API эндпоинты

### Каталог автомобилей (Che168)
- `GET /api/catalog/search` - Поиск авто (brand, model, min/max_price, min/max_year, engine_type, body_type)
- `GET /api/catalog/brands` - Список всех марок
- `GET /api/catalog/models/{brand}` - Модели для марки
- `GET /api/catalog/{car_id}` - Детали авто
- `POST /api/catalog/{car_id}/add-to-garage` - Добавить в гараж

### Заявки на подбор
- `POST /api/applications/create` - Создание заявки (новая расширенная схема ~70 полей)
- `GET /api/applications/my` - Список заявок пользователя
- `GET /api/applications/{id}` - Детали заявки
- `DELETE /api/applications/{id}` - Отмена заявки
- `POST /api/applications/{id}/request-manager-help` - Запрос помощи менеджера ($200)
- `POST /api/applications/{id}/start-tender` - Запуск тендера

### Сделки
- `POST /api/deals/{id}/skip-stage` - Пропуск этапа
- `POST /api/deals/{id}/pay-stage` - Оплата этапа
- `POST /api/deals/{id}/select-contractor` - Выбор подрядчика
- `GET /api/tracking/{id}` - GPS-отслеживание

### Подрядчики
- `POST /api/contractors/register` - Регистрация
- `POST /api/contractors/login` - Вход
- `GET /api/contractor-dashboard` - Дашборд

---

## Исправлено (11.03.2026)

### ✅ Баг: Цена для Беларуси не рассчитывалась при добавлении авто в гараж
**Проблема**: При добавлении авто в гараж поля `calculated_price_usd` и `calculated_price_byn` были пустыми.

**Решение**: Проверено и подтверждено, что функция `calculate_customs()` корректно вызывается в эндпоинте `POST /api/garage`. Тестирование показало:
- Электро авто: `calculated_price_usd: 35541.48`, `calculated_price_byn: 103894.84`
- ДВС авто: `calculated_price_usd: 34215.71`, `calculated_price_byn: 100019.37`

**Статус**: ✅ Работает (протестировано 13/13 тестов)

### ✅ Баг: bcrypt warning в логах
**Проблема**: `AttributeError: module 'bcrypt' has no attribute '__about__'` появлялась при каждой операции аутентификации.

**Причина**: Несовместимость passlib 1.7.4 с bcrypt 4.x (passlib пытается получить версию через `bcrypt.__about__.__version__`, которого нет в bcrypt 4.x).

**Решение**: Добавлено подавление предупреждения через:
```python
logging.getLogger("passlib.handlers.bcrypt").setLevel(logging.ERROR)
```
в файлах `/app/backend/server.py` и `/app/backend/utils/auth.py`.

**Статус**: ✅ Исправлено (предупреждения больше не появляются)

### ✅ Баг: "Автомобиль не найден" при добавлении авто из тендера в сделку
**Проблема**: При принятии предложения подрядчика по тендеру (созданному из заявки) и добавлении в сделку возникала ошибка "Автомобиль не найден".

**Причина**: Когда тендер создаётся из заявки (не из гаража), у него нет `car_id` и `car_info` - только `car_request` с параметрами желаемого авто. Эндпоинт `/deals/add-car` искал авто только в гараже.

**Решение** в `/app/backend/server.py`:
1. Если `car_id` не найден в гараже и это тендер, получаем данные из предложения подрядчика (`tender_offers`)
2. Дополняем данные о марке/модели из `car_request` тендера
3. Создаём запись в гараже автоматически из данных предложения
4. Обновлён фронтенд `/app/frontend/src/pages/dashboard/Tenders.js` - передаётся `tender_id`

**Статус**: ✅ Работает (авто из тендерного предложения успешно добавляется в сделку)

### ✅ Баг: Подрядчики не отображались при выборе для этапа "Доставка до порта (Китай)"
**Проблема**: При выборе подрядчика для этапа "Доставка до порта (Китай)" список подрядчиков был пустым.

**Причины**:
1. Фильтрация подрядчиков искала `services.includes('logistics')`, но у подрядчиков услуги указаны как `logistics_china`
2. Кнопка "Выбрать подрядчика" показывалась только для текущего этапа (`isCurrent`), а не для всех pending этапов
3. Если `contractor_id` был установлен, но `price = 0`, статус считался `contractor_selected` и кнопка не показывалась

**Решение** в `/app/frontend/src/pages/dashboard/DealCars.js`:
1. Расширена фильтрация подрядчиков для всех этапов (logistics_china, delivery_rb, customs, inspection, export)
2. Убрано условие `isCurrent` - кнопки показываются для всех pending этапов
3. Статус `contractor_selected` возвращается только если `contractor_id && price > 0`

**Статус**: ✅ Работает (подрядчики отображаются для всех этапов)

### ✅ Баг: Уведомления о тендерах не приходили подрядчикам
**Проблема**: При создании тендера подрядчики не получали уведомления о новых тендерах.

**Решение**:
1. В `/app/backend/server.py`:
   - Добавлена отправка уведомлений всем одобренным подрядчикам при создании тендера из заявки (endpoint `POST /api/applications/{id}/start-tender`)
   - Добавлена отправка уведомлений при создании тендера из гаража (endpoint `POST /api/tenders`)
   
2. В `/app/frontend/src/pages/ContractorDashboard.js`:
   - Добавлена иконка колокольчика с бейджем непрочитанных уведомлений
   - Добавлен выпадающий список уведомлений с типом, текстом и датой
   - Клик на уведомление о тендере переключает на вкладку "Тендеры"
   - Добавлена функция отметки уведомления как прочитанного

**Статус**: ✅ Работает (уведомления создаются и отображаются в UI)

### ✅ Баг: Каталог - не отображались параметры авто и не рассчитывалась цена
**Проблема**: В карточках каталога не показывались год, цена, объём двигателя. При добавлении авто из каталога в гараж не рассчитывалась цена для Беларуси.

**Причины**:
1. Frontend использовал `car.year` и `car.price` вместо `car.year_from` и `car.price_from_cny`
2. Объём двигателя показывался в мл вместо литров (1600 вместо 1.6L)
3. Эндпоинт `POST /api/catalog/{car_id}/add-to-garage` не вызывал функцию расчёта таможенных платежей
4. Поле `images` от API возвращалось как JSON-строка, а не массив

**Решение**:
1. Исправлен Frontend `/app/frontend/src/pages/dashboard/Catalog.js`:
   - `car.year_from || car.year` для года
   - `car.price_from_cny || car.price` для цены
   - `car.engine_volume / 1000` для объёма в литрах
2. Добавлен расчёт цены в `/app/backend/routes/catalog.py`:
   - Импортирован `calculate_customs_price` из `services.calculator`
   - Добавлен вызов расчёта при добавлении из каталога
   - Исправлен парсинг `images` JSON-строки
3. Добавлен proxy для китайских CDN в `/app/frontend/src/pages/dashboard/MyGarage.js`

**Статус**: ✅ Работает (карточки отображают все параметры, цена рассчитывается, изображения загружаются)

---

## Известные проблемы

1. **AI чат** - периодически не отвечает (P1)
2. **URL парсер** - блокируется анти-скрейпингом (P2)
3. **Баланс** - пополнение не реализовано (placeholder)

---

## Интеграции

- **Che168 API**: Реальные объявления авто из Китая через auto-api.com
  - API Key: `DQugK90Bo5ci1ZeDP6Wr`
  - Endpoint: `https://api1.auto-api.com/api/v2/che168`
- **Emergent LLM Key**: Используется для AI чата
- **MongoDB**: База данных
- **ReportLab**: Генерация PDF контрактов
- **Telegram Bot**: Уведомления пользователям
  - Bot username: `@KARBRIDGE_Bot`
  - Token: Хранится в `backend/.env`
  - Webhook: `https://vehicle-tender-hub.preview.emergentagent.com/api/telegram/webhook`

---

## Исправлено (13.03.2026)

### ✅ Telegram интеграция - ЗАВЕРШЕНО
**Дата**: 13.03.2026

**Реализовано:**
- Сервис уведомлений `backend/services/telegram_service.py`
- Endpoints для привязки/отвязки Telegram:
  - `POST /api/telegram/link` - генерация ссылки для привязки
  - `GET /api/telegram/status` - проверка статуса
  - `POST /api/telegram/test` - тестовое уведомление
  - `POST /api/telegram/unlink` - отвязка
  - `POST /api/contractor/telegram/link` - для подрядчиков
- Webhook для обработки сообщений бота
- UI компонент `TelegramSettings.js` в dashboard

**Уведомления отправляются при:**
- Новых сообщениях в чатах сделок
- Изменении статуса этапов
- Действиях модератора
- Новых тендерах (для подрядчиков)

**Тестирование:** ✅ 100% (10/10 backend, все frontend тесты)

### ✅ Двухсторонний обмен сообщениями через Telegram - ЗАВЕРШЕНО
**Дата**: 13.03.2026

**Реализовано:**
- Пользователи могут отвечать на уведомления прямо в Telegram
- Сообщения автоматически попадают в соответствующий чат этапа сделки
- При нескольких активных сделках показывается меню выбора чата (inline keyboard)
- После отправки сообщения приходит подтверждение доставки

**Новые команды бота:**
- `/chats` - показать список активных чатов
- `/cancel` - отменить выбор чата
- `/help` - справка по командам

**Новые функции в telegram_service.py:**
- `send_chat_selection()` - отправка inline keyboard с выбором чата
- `send_message_confirmation()` - подтверждение доставки сообщения
- `send_awaiting_message_prompt()` - запрос на ввод сообщения
- `answer_callback_query()` - обработка нажатий на кнопки
- `edit_message_text()` - редактирование сообщений

**Логика работы:**
1. Уведомление о новом сообщении содержит кнопку "Ответить"
2. При нажатии устанавливается контекст (deal_id + stage_key)
3. Следующее сообщение отправляется в выбранный чат
4. При одном активном чате - автовыбор

**Тестирование:** ✅ 100% (17/17 backend тестов)

### ✅ Исправлен z-index инфографики этапов
**Дата**: 13.03.2026

**Проблема:** Кнопки этапов в инфографике сделки были сложно кликабельны из-за перекрытия линией соединения.

**Решение:** Добавлен `z-30` на кнопки этапов в `StageInfographic.js`

### ✅ Исправлена загрузка .env для Telegram
**Дата**: 13.03.2026

**Проблема:** Токен Telegram бота не загружался из .env файла.

**Решение:** 
1. Исправлен формат .env (токен был склеен с другой переменной)
2. Добавлен `python-dotenv` в `telegram_service.py`
3. Обновлён webhook Telegram бота на актуальный URL


### ✅ Уведомления модераторам в Telegram - ЗАВЕРШЕНО
**Дата**: 13.03.2026

**Реализовано:**
Модераторы получают уведомления в Telegram о:
- 👤 Новых зарегистрированных пользователях
- 🏢 Новых заявках подрядчиков (ожидающих одобрения)
- 🔍 Этапах на проверке (pending_review)
- 📋 Запросах верификации пользователей
- 📝 Назначениях подрядчиков (ожидающих одобрения)
- 🆘 Запросах помощи менеджера ($200)
- 📄 Новых заявках на подбор авто
- 📢 Новых тендерах

**Новые функции в telegram_service.py:**
- `notify_moderators_new_user()` - новый пользователь
- `notify_moderators_contractor_approval()` - заявка подрядчика
- `notify_moderators_stage_review()` - этап на проверке
- `notify_moderators_verification_request()` - верификация
- `notify_moderators_prepayment_request()` - предоплата
- `notify_moderators_contractor_assignment()` - назначение подрядчика
- `notify_moderators_manager_help_request()` - запрос менеджера
- `notify_moderators_new_application()` - новая заявка
- `notify_moderators_new_tender()` - новый тендер

**Вспомогательная функция:**
- `get_moderator_chat_ids()` - получение chat_id всех модераторов/админов

### ✅ Автопривязка Telegram по username
**Дата**: 13.03.2026

**Реализовано:**
- При /start в боте автоматически ищется пользователь/подрядчик по @username
- Если найден — привязывается автоматически
- Админские endpoints для статистики: `GET /api/admin/telegram/stats`


### ✅ Улучшен AI-ассистент на главной странице
**Дата**: 13.03.2026

**Реализовано:**
AI-ассистент теперь:
- 🚗 Помогает с подбором авто через структурированный опрос
- 💬 Отвечает на вопросы о платформе CARBRIDGE
- 💰 Рассчитывает стоимость авто с доставкой и таможней
- 📋 Консультирует по 8 этапам сделки
- 📊 Показывает актуальные авто из каталога CHE168

**Формула расчёта стоимости:**
Цена в Китае + Доставка (~$2,000) + Таможня (50%) + Услуги платформы (5%)

**Frontend улучшения:**
- Добавлен таймаут 60 сек для AI-ответов
- Улучшена обработка ошибок с понятными сообщениями
- Сохраняется история чата в session_id


### ✅ Исправлено отображение Telegram-сообщений и файлов в чате
**Дата**: 26.03.2026

**Проблема:**
Сообщения и файлы, отправленные из Telegram, не отображались в чате платформы.

**Исправлено:**
1. **Этапный чат без подрядчика** — диалог этапа теперь показывает сообщения даже если подрядчик ещё не назначен (раньше чат был полностью скрыт)
2. **Индикатор Telegram** — добавлена метка "TG" в StageInfographic, ContractorMessenger и бейдж "Telegram" в Documents для сообщений из Telegram (`source: "telegram"`)
3. **Обработка файлов из Telegram** — вебхук теперь обрабатывает фото, документы, видео, аудио и голосовые сообщения из Telegram:
   - Скачивание файлов через Telegram Bot API (`getFile` → download)
   - Сохранение в `deal_files` коллекцию и на диск (`/app/backend/uploads/`)
   - Привязка к сообщению через поле `file_ids`
4. **Отображение файлов в чате** — кнопки скачивания файлов рядом с сообщениями

**Изменённые файлы:**
- `/app/backend/server.py` — `download_telegram_file()`, `save_telegram_file_to_deal()`, обновлён webhook handler
- `/app/frontend/src/components/deal/StageInfographic.js` — чат без подрядчика, TG индикатор, файлы
- `/app/frontend/src/components/deal/ContractorMessenger.js` — TG индикатор, файлы
- `/app/frontend/src/pages/dashboard/Documents.js` — Telegram бейдж, файлы


### ✅ Помощь менеджера — назначение и чат
**Дата**: 26.03.2026

**Реализовано:**
1. **Клиент**: Кнопка "Создать запрос" во вкладке "Помощь менеджера" (Документы) с описанием проблемы
2. **Администратор**: Вкладка "Помощь менеджера" в панели модератора — список всех запросов, назначение менеджера из модераторов/админов, чат с клиентом, закрытие запроса
3. **Чат**: Двусторонний обмен сообщениями между клиентом и назначенным менеджером
4. **Telegram-уведомления**: При создании запроса, назначении менеджера и новых сообщениях

**API эндпоинты:**
- `POST /api/help-requests` — создание запроса
- `GET /api/help-requests` — запросы пользователя
- `POST /api/help-requests/{id}/messages` — сообщение клиента
- `GET /api/moderator/help-requests` — все запросы (модератор)
- `GET /api/moderator/available-managers` — список менеджеров
- `POST /api/moderator/help-requests/{id}/assign` — назначение менеджера
- `POST /api/moderator/help-requests/{id}/messages` — сообщение менеджера
- `POST /api/moderator/help-requests/{id}/close` — закрытие запроса


### ✅ Расширенная форма предложений подрядчика с файлами и карточка авто для клиента
**Дата**: 26.03.2026

**Реализовано:**
1. **Форма подрядчика**: загрузка фото/видео файлами (до 50MB), структурированные поля авто (марка, модель, год, пробег, двигатель, объём, цвет, КПП, VIN)
2. **Карточка предложения у клиента**: галерея фото с навигацией, характеристики авто, цена, этапы в цене, информация о подрядчике
3. **Профиль подрядчика**: клиент может просмотреть полный профиль (компания, город, услуги, рейтинг, статистика завершённых сделок)
4. **Новые API**: `POST /api/contractor-offers/{id}/files`, `GET /api/offers/{id}/files`, `GET /api/offer-files/{id}/download`, `GET /api/contractors/{id}/public-profile`

### ✅ Исправлена логика видимости заявок для подрядчиков
**Дата**: 26.03.2026

**Проблема:**
Заявки клиентов отображались у подрядчиков сразу при создании, хотя по бизнес-логике заявка — это подготовительный этап к тендеру. Подрядчики должны видеть только тендеры (после нажатия клиентом "Запустить тендер").

**Исправлено:**
1. Убрана вкладка "Заявки клиентов" из кабинета подрядчика
2. Убрана загрузка `applications` из API `/api/contractor-dashboard`
3. Добавлен счётчик "Активные сделки" вместо "Новые заявки"
4. Обогащены данные тендера при создании из заявки — теперь включают: бюджет в Китае, общий бюджет, привод, пробег, сроки, опции, город доставки и др.
5. Расширено отображение тендеров: 8 параметров, доп. требования, опции клиента

**Проблема:**
Скачивание файлов не работало ни в одном чате (Клиент, Модератор, Подрядчик). Браузер блокировал программатическое создание blob URL и клик по ссылке внутри сложных React-компонентов.

**Корневые причины:**
1. Endpoint `/api/files/{file_id}/public-download` использовал несуществующие переменные `SECRET_KEY` и `ALGORITHM` вместо `JWT_SECRET` и `JWT_ALGORITHM` — вызывал NameError
2. Endpoint не поддерживал токены подрядчиков (type=contractor)
3. Все 4 фронтенд компонента использовали устаревший blob-подход: `fetch → blob → createObjectURL → link.click()`

**Решение:**
1. **Бэкенд** (`server.py`): Исправлены переменные JWT, добавлена поддержка contractor-токенов с проверкой назначения на этап сделки
2. **Documents.js**: `downloadFile` → `window.open(url + "?token=JWT", "_blank")`
3. **StageInfographic.js**: Аналогичная замена
4. **ContractorMessenger.js**: Аналогичная замена
5. **ModeratorPage.js**: `downloadStageFile` и `FileImagePreview` обновлены — превью и скачивание через публичный endpoint

**Тестирование:** ✅ 100% (8/8 backend, 4/4 frontend компонента)
